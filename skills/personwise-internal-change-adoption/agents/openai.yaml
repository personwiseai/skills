interface:
  display_name: Internal Change Adoption Course
  short_description: Turn change communications into an askable digital-human adoption course.
  default_prompt: Use $personwise-internal-change-adoption to build an internal change adoption course from our rollout communications.
dependencies:
  tools:
  - type: mcp
    value: personwise-course-creation
    description: Create and publish PersonWise courses with OAuth
    transport: streamable_http
    url: https://mcp.personwise.ai/mcp
policy:
  allow_implicit_invocation: true
