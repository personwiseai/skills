interface:
  display_name: Product First-Success Guide
  short_description: Turn setup guides into an askable digital-human quick-start course.
  default_prompt: Use $personwise-product-first-success to build a quick start course from our setup documentation.
dependencies:
  tools:
  - type: mcp
    value: personwise-course-creation
    description: Create and publish PersonWise courses with OAuth
    transport: streamable_http
    url: https://mcp.personwise.ai/mcp
policy:
  allow_implicit_invocation: true
