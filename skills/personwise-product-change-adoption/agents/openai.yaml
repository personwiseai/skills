interface:
  display_name: Product Change Adoption Course
  short_description: Turn release notes into an askable digital-human adoption course.
  default_prompt: Use $personwise-product-change-adoption to build a migration adoption course from our release notes.
dependencies:
  tools:
  - type: mcp
    value: personwise-course-creation
    description: Create and publish PersonWise courses with OAuth
    transport: streamable_http
    url: https://mcp.personwise.ai/mcp
policy:
  allow_implicit_invocation: true
