interface:
  display_name: Developer Quickstart
  short_description: Turn API docs into an askable digital-human first-call course.
  default_prompt: Use $personwise-developer-quickstart to build a developer quickstart course from our API documentation.
dependencies:
  tools:
  - type: mcp
    value: personwise-course-creation
    description: Create and publish PersonWise courses with OAuth
    transport: streamable_http
    url: https://mcp.personwise.ai/mcp
policy:
  allow_implicit_invocation: true
