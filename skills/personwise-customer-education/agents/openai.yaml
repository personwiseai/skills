interface:
  display_name: Customer Education
  short_description: Turn support knowledge into an askable digital-human customer course.
  default_prompt: Use $personwise-customer-education to build a customer education course from our help content.
dependencies:
  tools:
  - type: mcp
    value: personwise-course-creation
    description: Create and publish PersonWise courses with OAuth
    transport: streamable_http
    url: https://mcp.personwise.ai/mcp
policy:
  allow_implicit_invocation: true
